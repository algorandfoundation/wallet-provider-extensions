/**
 * React Native condition entry for `@algorandfoundation/credentials`.
 *
 * Resolved via the `react-native` export condition; delegates to
 * `@algorandfoundation/react-native-credentials`, which re-exports the
 * platform-neutral core alongside the React Native `WithCredentials`
 * extension and the React Native Digital Credentials seam.
 */

export * from "@algorandfoundation/react-native-credentials";
