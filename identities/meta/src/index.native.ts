/**
 * React Native condition entry for `@algorandfoundation/identities`.
 *
 * Resolved via the `react-native` export condition (honored by Metro).
 * Currently identical to the platform-neutral composition (store + unified
 * `WithIdentities`); a React Native identities package will replace this
 * delegation when a real platform seam lands.
 */

export * from "@algorandfoundation/identities-store";
export * from "@algorandfoundation/identities-extension";
