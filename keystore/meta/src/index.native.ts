/**
 * React Native condition entry for `@algorandfoundation/keystore`.
 *
 * Resolved via the `react-native` export condition; delegates to
 * `@algorandfoundation/react-native-keystore`, which re-exports the
 * platform-neutral core alongside the React Native specific `WithKeyStore`
 * extension and biometric-backed storage.
 */

export * from "@algorandfoundation/react-native-keystore";
